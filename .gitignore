# Rust build artifacts
/target/
**/*.rs.bk

# insta review queue (committed snapshots have no .new suffix)
**/*.snap.new
**/*.pending-snap

# Editor / OS
.DS_Store
*.swp
/.idea/
/.vscode/

# Legacy pre-implementation design drafts — kept locally, intentionally NOT
# published (superseded by docs/superpowers/specs/). Untracked, not deleted.
/CUDA-Version-Manager-Research.md
/CVM-ADRs.md
/CVM-Design-Document.md
